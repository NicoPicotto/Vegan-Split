import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    marginBottom: 15,
    elevation: 5,
  },
  textButton: {
    fontSize: 16,
    paddingHorizontal: 10,
    paddingVertical: 7,
    borderRadius: 20,
  },
});